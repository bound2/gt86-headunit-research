cmake_minimum_required(VERSION 3.21)
project(gt86_firmware_research LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
add_library(firmware_reader src/firmware.cpp)
target_include_directories(firmware_reader PUBLIC src)
add_executable(fwinspect src/main.cpp)
target_link_libraries(fwinspect PRIVATE firmware_reader)
add_library(minilzo STATIC third_party/lzo-2.10/minilzo/minilzo.c)
target_include_directories(minilzo PUBLIC third_party/lzo-2.10/minilzo third_party/lzo-2.10/include/lzo)
add_library(qnx_reader src/qnx.cpp)
target_link_libraries(qnx_reader PUBLIC firmware_reader PRIVATE minilzo)
add_executable(qnxinspect src/qnx_main.cpp)
target_link_libraries(qnxinspect PRIVATE qnx_reader)
if(MSVC)
  target_compile_options(firmware_reader PRIVATE /W4 /permissive-)
  target_compile_options(fwinspect PRIVATE /W4 /permissive-)
  target_compile_options(qnx_reader PRIVATE /W4 /permissive-)
  target_compile_options(qnxinspect PRIVATE /W4 /permissive-)
endif()
enable_testing()
add_library(carplay_protocol STATIC src/carplay/iap2_wire.c src/carplay/iap2_auth.c src/carplay/iap2_link.c src/carplay/iap2_control.c src/carplay/iap2_identification.c src/carplay/iap2_transport.c src/carplay/iap2_carplay.c src/carplay/iap2_power.c src/carplay/usbmux_wire.c src/carplay/usbmux_host.c src/carplay/usbmux_connection.c src/carplay/usbmux_dispatcher.c)
set_target_properties(carplay_protocol PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
target_sources(carplay_protocol PRIVATE src/carplay/lockdown_wire.c src/carplay/lockdown_channel.c)
target_sources(carplay_protocol PRIVATE src/carplay/service_plist.c src/carplay/lockdown_reply.c)
target_sources(carplay_protocol PRIVATE src/carplay/lockdown_bootstrap.c)
target_sources(carplay_protocol PRIVATE src/carplay/rtsp_wire.c src/carplay/rtsp_channel.c)
target_sources(carplay_protocol PRIVATE src/carplay/pair_tlv.c)
target_include_directories(carplay_protocol PUBLIC src/carplay)
if(MSVC)
  target_compile_options(carplay_protocol PRIVATE /W4)
else()
  target_compile_options(carplay_protocol PRIVATE -Wall -Wextra -Wpedantic)
endif()
add_executable(iap2_tests tests/iap2_tests.cpp)
target_link_libraries(iap2_tests PRIVATE carplay_protocol)
add_test(NAME iap2_tests COMMAND iap2_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/iap2-csm-vectors.txt")
add_executable(iap2_link_tests tests/iap2_link_tests.cpp)
target_link_libraries(iap2_link_tests PRIVATE carplay_protocol)
add_test(NAME iap2_link_tests COMMAND iap2_link_tests)
add_executable(iap2_control_tests tests/iap2_control_tests.cpp)
target_link_libraries(iap2_control_tests PRIVATE carplay_protocol)
add_test(NAME iap2_control_tests COMMAND iap2_control_tests)
add_executable(iap2_identification_tests tests/iap2_identification_tests.cpp)
target_link_libraries(iap2_identification_tests PRIVATE carplay_protocol)
add_test(NAME iap2_identification_tests COMMAND iap2_identification_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/iap2-csm-vectors.txt")
add_executable(iap2_transport_tests tests/iap2_transport_tests.cpp)
target_link_libraries(iap2_transport_tests PRIVATE carplay_protocol)
add_test(NAME iap2_transport_tests COMMAND iap2_transport_tests)
add_executable(iap2_carplay_tests tests/iap2_carplay_tests.cpp)
target_link_libraries(iap2_carplay_tests PRIVATE carplay_protocol)
add_test(NAME iap2_carplay_tests COMMAND iap2_carplay_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/iap2-csm-vectors.txt")
add_executable(usbmux_tests tests/usbmux_tests.cpp)
target_link_libraries(usbmux_tests PRIVATE carplay_protocol)
add_test(NAME usbmux_tests COMMAND usbmux_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/usbmux-wire-vectors.txt")
add_executable(usbmux_host_tests tests/usbmux_host_tests.cpp)
target_link_libraries(usbmux_host_tests PRIVATE carplay_protocol)
add_test(NAME usbmux_host_tests COMMAND usbmux_host_tests)
add_executable(usbmux_connection_tests tests/usbmux_connection_tests.cpp)
target_link_libraries(usbmux_connection_tests PRIVATE carplay_protocol)
add_test(NAME usbmux_connection_tests COMMAND usbmux_connection_tests)
add_executable(usbmux_dispatcher_tests tests/usbmux_dispatcher_tests.cpp)
target_link_libraries(usbmux_dispatcher_tests PRIVATE carplay_protocol)
add_test(NAME usbmux_dispatcher_tests COMMAND usbmux_dispatcher_tests)
add_executable(lockdown_tests tests/lockdown_tests.cpp)
target_link_libraries(lockdown_tests PRIVATE carplay_protocol)
add_test(NAME lockdown_tests COMMAND lockdown_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
add_executable(service_plist_tests tests/service_plist_tests.cpp)
target_link_libraries(service_plist_tests PRIVATE carplay_protocol)
add_test(NAME service_plist_tests COMMAND service_plist_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
add_executable(lockdown_bootstrap_tests tests/lockdown_bootstrap_tests.cpp)
target_link_libraries(lockdown_bootstrap_tests PRIVATE carplay_protocol)
add_test(NAME lockdown_bootstrap_tests COMMAND lockdown_bootstrap_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
add_executable(rtsp_tests tests/rtsp_tests.cpp)
target_link_libraries(rtsp_tests PRIVATE carplay_protocol)
add_test(NAME rtsp_tests COMMAND rtsp_tests)
add_executable(pair_tlv_tests tests/pair_tlv_tests.cpp)
target_link_libraries(pair_tlv_tests PRIVATE carplay_protocol)
add_test(NAME pair_tlv_tests COMMAND pair_tlv_tests)

# Bounded capability serializer is portable but kept separate from the existing
# import-free ARM core claim until its own target/runtime review.
add_library(carplay_info STATIC src/carplay/projection_info.c)
set_target_properties(carplay_info PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
target_link_libraries(carplay_info PUBLIC carplay_protocol)
if(MSVC)
  target_compile_options(carplay_info PRIVATE /W4)
else()
  target_compile_options(carplay_info PRIVATE -Wall -Wextra -Wpedantic)
endif()
add_executable(projection_info_tests tests/projection_info_tests.cpp)
target_link_libraries(projection_info_tests PRIVATE carplay_info)
add_test(NAME projection_info_tests COMMAND projection_info_tests)

# Optional, separately pinned pairing primitives; no TLS or OS RNG dependency.
set(CARPLAY_MONOCYPHER_SOURCE "" CACHE PATH "Pinned Monocypher 4.0.3 source (scripts/Prepare-CarPlayCrypto.ps1)")
if(CARPLAY_MONOCYPHER_SOURCE)
  # Verify even when configuring directly instead of using the preparation script.
  set(_pair_source_pins
    "src/monocypher.c|57eb914fc88136119bd41655cccb8c250048bf54d470540625186f8ab16f64be"
    "src/monocypher.h|c494da712122da7ff679fdcf318a5317e84972b6c950fe9d896212947797facd"
    "src/optional/monocypher-ed25519.c|60fce3578fb00b00da96490653d993c4cb427b1e1be38183285c66e04d22cc18"
    "src/optional/monocypher-ed25519.h|abc4fad381879f5c29176ebe014b9189956b3dfe0a3e36459b6990bc57212380")
  foreach(_pair_pin IN LISTS _pair_source_pins)
    string(REPLACE "|" ";" _pair_parts "${_pair_pin}")
    list(GET _pair_parts 0 _pair_file)
    list(GET _pair_parts 1 _pair_expected)
    file(SHA256 "${CARPLAY_MONOCYPHER_SOURCE}/${_pair_file}" _pair_actual)
    if(NOT _pair_actual STREQUAL _pair_expected)
      message(FATAL_ERROR "Monocypher 4.0.3 source mismatch: ${_pair_file}")
    endif()
  endforeach()
  add_library(carplay_monocypher STATIC "${CARPLAY_MONOCYPHER_SOURCE}/src/monocypher.c"
    "${CARPLAY_MONOCYPHER_SOURCE}/src/optional/monocypher-ed25519.c")
  target_include_directories(carplay_monocypher PUBLIC "${CARPLAY_MONOCYPHER_SOURCE}/src"
    "${CARPLAY_MONOCYPHER_SOURCE}/src/optional")
  set_target_properties(carplay_monocypher PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  add_library(carplay_pairing STATIC src/carplay/pair_crypto.c src/carplay/pair_verify.c
    src/carplay/control_cipher.c src/carplay/projection_control.c src/carplay/pair_store.c)
  set_target_properties(carplay_pairing PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_pairing PUBLIC carplay_protocol carplay_monocypher)
  if(MSVC)
    target_compile_options(carplay_pairing PRIVATE /W4)
  else()
    target_compile_options(carplay_pairing PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(pair_crypto_tests tests/pair_crypto_tests.cpp)
  target_link_libraries(pair_crypto_tests PRIVATE carplay_pairing)
  add_test(NAME pair_crypto_tests COMMAND pair_crypto_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-verify-vectors.txt")
  add_executable(control_cipher_tests tests/control_cipher_tests.cpp)
  target_link_libraries(control_cipher_tests PRIVATE carplay_pairing)
  add_test(NAME control_cipher_tests COMMAND control_cipher_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/control-cipher-vectors.txt")
  add_executable(projection_control_tests tests/projection_control_tests.cpp)
  target_link_libraries(projection_control_tests PRIVATE carplay_pairing)
  add_test(NAME projection_control_tests COMMAND projection_control_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-verify-vectors.txt")
  add_executable(pair_store_tests tests/pair_store_tests.cpp)
  target_link_libraries(pair_store_tests PRIVATE carplay_pairing)
  add_test(NAME pair_store_tests COMMAND pair_store_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-verify-vectors.txt")
  if(WIN32)
    add_library(carplay_pair_store_file STATIC src/carplay/pair_store_file.c)
    set_target_properties(carplay_pair_store_file PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
    target_link_libraries(carplay_pair_store_file PUBLIC carplay_pairing advapi32)
    if(MSVC)
      target_compile_options(carplay_pair_store_file PRIVATE /W4)
    else()
      target_compile_options(carplay_pair_store_file PRIVATE -Wall -Wextra -Wpedantic)
    endif()
  endif()
endif()

# Optional hosted cryptography; never silently replace the freestanding target.
set(CARPLAY_MBEDTLS_SOURCE "" CACHE PATH "Pinned Mbed TLS 3.6.7 release source (see scripts/Prepare-CarPlayTls.ps1)")
if(CARPLAY_MBEDTLS_SOURCE)
  set(ENABLE_PROGRAMS OFF CACHE BOOL "" FORCE)
  set(ENABLE_TESTING OFF CACHE BOOL "" FORCE)
  set(MBEDTLS_USER_CONFIG_FILE "${CMAKE_SOURCE_DIR}/src/carplay/tls_config.h" CACHE FILEPATH "" FORCE)
  add_subdirectory("${CARPLAY_MBEDTLS_SOURCE}" mbedtls EXCLUDE_FROM_ALL)
  add_library(carplay_tls STATIC src/carplay/lockdown_tls.c src/carplay/lockdown_client.c src/carplay/carkit.c src/carplay/carkit_iap2.c)
  set_target_properties(carplay_tls PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_tls PUBLIC carplay_protocol mbedtls)
  if(MSVC)
    target_compile_options(carplay_tls PRIVATE /W4)
  else()
    target_compile_options(carplay_tls PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(lockdown_tls_tests tests/lockdown_tls_tests.cpp)
  target_link_libraries(lockdown_tls_tests PRIVATE carplay_tls)
  add_test(NAME lockdown_tls_tests COMMAND lockdown_tls_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
  add_executable(carkit_tests tests/carkit_tests.cpp)
  target_link_libraries(carkit_tests PRIVATE carplay_tls)
  add_test(NAME carkit_tests COMMAND carkit_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
  add_executable(carkit_iap2_tests tests/carkit_iap2_tests.cpp)
  target_link_libraries(carkit_iap2_tests PRIVATE carplay_tls)
  add_test(NAME carkit_iap2_tests COMMAND carkit_iap2_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/lockdown")
endif()
# Optional hosted enrollment: SRP MPI is deliberately separate from the
# Monocypher-only pairing target and its ARM portability check.
if(CARPLAY_MONOCYPHER_SOURCE AND CARPLAY_MBEDTLS_SOURCE)
  add_library(carplay_projection_auth STATIC src/carplay/mfi_sap.c src/carplay/projection_auth.c)
  set_target_properties(carplay_projection_auth PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_projection_auth PUBLIC carplay_pairing mbedcrypto)
  if(MSVC)
    target_compile_options(carplay_projection_auth PRIVATE /W4)
  else()
    target_compile_options(carplay_projection_auth PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(mfi_sap_tests tests/mfi_sap_tests.cpp)
  target_link_libraries(mfi_sap_tests PRIVATE carplay_projection_auth carplay_enrollment)
  add_test(NAME mfi_sap_tests COMMAND mfi_sap_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/mfi-sap-vectors.txt" "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-setup-vectors.txt")
  add_library(carplay_enrollment STATIC src/carplay/pair_srp.c src/carplay/pair_setup.c src/carplay/pair_setup_channel.c)
  set_target_properties(carplay_enrollment PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_enrollment PUBLIC carplay_pairing carplay_projection_auth mbedcrypto)
  if(MSVC)
    target_compile_options(carplay_enrollment PRIVATE /W4)
  else()
    target_compile_options(carplay_enrollment PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(pair_setup_tests tests/pair_setup_tests.cpp)
  target_link_libraries(pair_setup_tests PRIVATE carplay_enrollment)
  add_test(NAME pair_setup_tests COMMAND pair_setup_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-setup-vectors.txt")
  add_library(carplay_session STATIC src/carplay/projection_session.c)
  set_target_properties(carplay_session PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_session PUBLIC carplay_pairing carplay_info)
  if(MSVC)
    target_compile_options(carplay_session PRIVATE /W4)
  else()
    target_compile_options(carplay_session PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(projection_session_tests tests/projection_session_tests.cpp)
  target_link_libraries(projection_session_tests PRIVATE carplay_session)
  add_test(NAME projection_session_tests COMMAND projection_session_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/projection-session-vectors.txt")
  add_library(carplay_receiver STATIC src/carplay/projection_receiver.c)
  set_target_properties(carplay_receiver PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  target_link_libraries(carplay_receiver PUBLIC carplay_enrollment carplay_projection_auth carplay_info carplay_session)
  if(MSVC)
    target_compile_options(carplay_receiver PRIVATE /W4)
  else()
    target_compile_options(carplay_receiver PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_executable(projection_receiver_tests tests/projection_receiver_tests.cpp)
  target_link_libraries(projection_receiver_tests PRIVATE carplay_receiver)
  add_test(NAME projection_receiver_tests COMMAND projection_receiver_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/mfi-sap-vectors.txt" "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-setup-vectors.txt" "${CMAKE_SOURCE_DIR}/tests/fixtures/projection-session-vectors.txt")
  if(WIN32)
    # Fault hooks exist only in this separate test backend, never the production
    # library. Tests still perform real file writes/flushes on public fixtures.
    add_library(carplay_pair_store_file_test STATIC src/carplay/pair_store_file.c)
    set_target_properties(carplay_pair_store_file_test PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
    target_compile_definitions(carplay_pair_store_file_test PUBLIC PAIR_STORE_TESTING=1)
    target_link_libraries(carplay_pair_store_file_test PUBLIC carplay_pairing advapi32)
    add_executable(pair_store_file_tests tests/pair_store_file_tests.cpp)
    target_link_libraries(pair_store_file_tests PRIVATE carplay_pair_store_file_test carplay_enrollment)
    add_test(NAME pair_store_file_tests COMMAND pair_store_file_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-setup-vectors.txt" "${CMAKE_BINARY_DIR}")
    # Run the same normal/security/enrollment paths against the actual production
    # library as well, where interruption hooks are not even declared/exported.
    add_executable(pair_store_file_production_tests tests/pair_store_file_tests.cpp)
    target_link_libraries(pair_store_file_production_tests PRIVATE carplay_pair_store_file carplay_enrollment)
    add_test(NAME pair_store_file_production_tests COMMAND pair_store_file_production_tests "${CMAKE_SOURCE_DIR}/tests/fixtures/pair-setup-vectors.txt" "${CMAKE_BINARY_DIR}")
  endif()
endif()
add_executable(reader_tests tests/reader_tests.cpp)
target_link_libraries(reader_tests PRIVATE firmware_reader)
add_test(NAME reader_tests COMMAND reader_tests)
add_executable(qnx_tests tests/qnx_tests.cpp)
target_link_libraries(qnx_tests PRIVATE qnx_reader minilzo)
add_test(NAME qnx_tests COMMAND qnx_tests)
if(WIN32)
  add_test(NAME qnx_extract_tests COMMAND powershell.exe -NoProfile -ExecutionPolicy Bypass
    -File "${CMAKE_SOURCE_DIR}/tests/qnx_extract_tests.ps1"
    -Inspector "$<TARGET_FILE:qnxinspect>" -FixtureGenerator "$<TARGET_FILE:qnx_tests>")
  add_test(NAME zip_tests COMMAND powershell.exe -NoProfile -ExecutionPolicy Bypass
    -File "${CMAKE_SOURCE_DIR}/tests/zip_tests.ps1" -BuildDirectory "${CMAKE_BINARY_DIR}")
endif()
